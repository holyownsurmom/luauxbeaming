SITE_URL=https://luaux.wtf
WORKER_SECRET=must-match-site-WORKER_SECRET
WORKER_ID=worker-1
POLL_INTERVAL_MS=3000
MAX_CONCURRENT_JOBS=8
PAYMENT_WATCH_MS=30000
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Firstmail — unique mailbox per secured account (type=3)
FIRSTMAIL_API_KEY=

# Optional catch-all fallback (unique address, shared IMAP)
# RECOVERY_MAIL_DOMAIN=
# RECOVERY_IMAP_HOST=
# RECOVERY_IMAP_USER=
# RECOVERY_IMAP_PASS=
# RECOVERY_IMAP_PORT=993
